# Alex-Hou-2024-test-17

Leptos + Axum full-stack starter application bootstrapped with `cargo-leptos`.

## Run locally

```bash
cargo leptos watch
```

The SSR server binds to `0.0.0.0:8080` and serves the hydrated client bundle generated by `cargo-leptos`.

## Build

```bash
cargo leptos build
```
